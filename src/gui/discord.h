#pragma once

// Аватарка и ник из Дискорда без ключей. Как работает:
//  - читаем id+хэш аватара из локальных сжатых баз дискорда (свой парсер,
//    только публичные поля — токены не трогаем даже рядом);
//  - картинку качаем с CDN дискорда фоном, Start не ждёт;
//  - Avatar дёргаем каждый кадр из user::Get.
// Ничего не нашли — тихо ничего не делаем, остаётся картинка винды.

namespace discord {

void Start(void* device); // ID3D11Device*, зовёт user::Init
const char* Name();       // nullptr пока нет
void* Avatar();           // SRV или nullptr (загрузка текстуры — тут же, главный поток)
void Shutdown();

} // namespace discord
